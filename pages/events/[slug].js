import Layout from '@/components/Layout';

const EventPage = () => {
  const title = 'Event title';
  return (
    <Layout title={title}>
      <h1>My event</h1>
    </Layout>
  );
};

export default EventPage;
